<UserControl x:Class="WpfApp.PlayerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="400">
    <Grid Margin="4" MouseDown="UserControl_MouseDown">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Image Grid.Row="0" Width="50" Height="50" Source="{Binding ImagePath}"
               Stretch="UniformToFill"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="{Binding ShirtNumber}" FontWeight="Bold" Margin="0,0,2,0"/>
            <TextBlock Text="{Binding Name}"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Text="★" Visibility="{Binding Captain, Converter={StaticResource BoolToVisibility}}"
                   Foreground="Gold" FontWeight="Bold" HorizontalAlignment="Center"/>
    </Grid>
</UserControl>
