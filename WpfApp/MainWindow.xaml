<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!--  Screen size  -->
        <StackPanel
        x:Name="spScreenSize"
        Grid.Row="0"
        Grid.Column="0"
        IsEnabled="False"
        VerticalAlignment="Center"
        HorizontalAlignment="Center"
            Margin="10">
            <Label
        x:Name="label4"
        Grid.Row="0"
        >Screen Size</Label>
            <RadioButton Margin="0 2 0 2" x:Name="rbSizeFullscreen">
                Fullscreen
            </RadioButton>
            <RadioButton Margin="0 2 0 2" x:Name="rbSize720px">
                1280x720
            </RadioButton>
            <RadioButton Margin="0 2 0 2" x:Name="rbSize480px">
                854x480
            </RadioButton>
            <RadioButton Margin="0 2 0 2" x:Name="rbSize360px">
                640x360
            </RadioButton>
        </StackPanel>
        <!-- Language -->
        <StackPanel
    Grid.Row="1"
    Grid.Column="0"
    VerticalAlignment="Center"
    HorizontalAlignment="Center"
        Margin="10"
    >
            <Label x:Name="lbChooseLanguage" Content="Language" />
            <RadioButton Margin="0 2 0 2" x:Name="rbEng" Content="English" Checked="rbEng_Checked"/>
            <RadioButton Margin="0 2 0 2" x:Name="rbCro" Content="Croatian" Checked="rbCro_Checked"/>

        </StackPanel>
        <!-- Champ -->
        <StackPanel
    Grid.Row="0"
    Grid.Column="1"
    VerticalAlignment="Center"
    HorizontalAlignment="Center"
    >
            <Label x:Name="lbChooseChampionship"  Content="Championship"/>
            <RadioButton Margin="0 2 0 2" x:Name="rbFemale"  Content="Female" Checked="rbFemale_Checked"/>
            <RadioButton Margin="0 2 0 2" x:Name="rbMale"  Content="Male" Checked="rbMale_Checked"/>
        </StackPanel>
        <!-- Representation -->
        <StackPanel
    x:Name="spRepresentation"
    Grid.Row="1"
    Grid.Column="1"
    VerticalAlignment="Center"
    HorizontalAlignment="Center"
    >
            <Label 
        x:Name="labelrep"
        VerticalAlignment="Center"
        HorizontalAlignment="Center" 
                Content="Home Representation"/>
            <ComboBox
        x:Name="cbRepresentation"
        VerticalAlignment="Top"
        HorizontalAlignment="Center"
        MinWidth="200"
                SelectionChanged="OnHomeRepresentationChanged"
    >
            </ComboBox>
            <Label x:Name="lbRepresentationHost"/>
            <Label 
        x:Name="lbGuest"
        VerticalAlignment="Center"
        HorizontalAlignment="Center"
        Content="Guest Representation"/>
            <ComboBox
        x:Name="cbRepresentationGuest"
        VerticalAlignment="Top"
        HorizontalAlignment="Center"
        MinWidth="200"
                SelectionChanged="OnRepresentationChanged"/>
            <Label x:Name="lbRepresentationGuest"/>
        </StackPanel>
        <Button
        x:Name="btnLandingContinue"
        Content="Continue"
        HorizontalAlignment="Right"
        VerticalAlignment="Bottom"
        Margin="0,0,20,20"
        Width="75"
        Click="btnContinue_Click" 
        Grid.Column="1" 
        Grid.Row="1"/>
    </Grid>
</Window>
