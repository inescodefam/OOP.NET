<Window x:Class="WpfApp.PlayerDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="PlayerDEtailsWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Image Source="{Binding ImagePath}" Width="100" Height="100" Grid.Row="0"/>
        <TextBlock Text="{Binding Name}" FontSize="18" Grid.Row="1"/>
        <TextBlock Text="{Binding ShirtNumber}" FontSize="16" Grid.Row="2"/>
        <TextBlock Text="Captain" Visibility="{Binding Captain, Converter={StaticResource BoolToVisibility}}" 
                   Grid.Row="3" Foreground="Green"/>
    </Grid>
</Window>
